MVP (MUSI BYĆ)

Tu zostaw tylko te rzeczy, które blokują uruchomienie:
	•	Email templates w Resend (bo bez tego UX jest martwy)
	•	ewentualnie krytyczne walidacje availability, jeśli nie masz overlapów
	•	auto-cleanup pending (jeśli nie chcesz “wiszących” slotów)

TODO LATER (PO MVP) — TYLKO NIEZROBIONE

1. Availability — walidacja slotów
	•	Walidacja, że startTime < endTime przy tworzeniu slotu.
	•	Walidacja, że nowe sloty nie nakładają się na istniejące sloty danego terapeuty.
	•	(Opcjonalnie) minimalna / maksymalna długość slotu (np. 50–60 min).

Filtrowanie tylko przyszłych slotów → JUŻ JEST.

⸻

2. Meetings — widoki / scope’y pod dashboard
	•	Dedykowane scope’y:
	•	upcoming → pending, confirmed
	•	past → completed, cancelled_by_user, cancelled_by_doc
	•	Opcjonalne dodatkowe scope’y:
	•	tylko cancelled
	•	tylko completed
	•	Jeden endpoint pod dashboard terapeuty (zamiast kilku zapytań).

⸻

3. Payments — funkcje po MVP
	•	Obsługa refundów (API providera).
	•	Rozróżnienie powodów refundu:
	•	user / therapist / system (TTL).
	•	Historia płatności w dashboardzie terapeuty.
	•	Saldo terapeuty + payouty (osobny etap).

Idempotencja, sanity checks, blokady statusów → JUŻ ZROBIONE.

⸻

4. Email / komunikacja — rozszerzenia
	•	Flow zmiany emaila użytkownika:
	•	mail potwierdzający nowy adres,
	•	update w bazie dopiero po kliknięciu.
	•	Mail do terapeuty po akceptacji profilu przez admina.
	•	Przypomnienia o spotkaniu (np. 24h przed terminem).
	•	(Opcjonalnie) rate limit per email, nie tylko per IP.

Rejestracja, reset hasła, booking, cancel → logika backendowa JUŻ JEST
(template’y i copy pozostają osobnym zadaniem operacyjnym).

⸻

5. Images / avatar
	•	Walidacja rozmiaru uploadu (np. max 2MB).
	•	Walidacja MIME (image/jpeg, image/png).
	•	Automatyczny resize (np. Sharp).
	•	Opcjonalne miniaturki.
	•	Czyszczenie nieużywanych plików (cron / lifecycle).

⸻

6. Profiles / DocProfile — weryfikacja i publiczny profil
	•	Publiczny endpoint:
	•	GET /profiles/doc/:id — publiczna karta terapeuty.
	•	Dokumenty terapeuty:
	•	upload dokumentów (S3 / Cloudflare R2),
	•	lista / usuwanie,
	•	podgląd dokumentów przez admina.
	•	Workflow weryfikacji:
	•	submit → JUŻ JEST
	•	approve / reject → JUŻ JEST (API)
	•	LATER: UI admina + wymagania dot. typów dokumentów.
	•	Doprecyzowanie reguł:
	•	które zmiany po approved wymagają ponownej weryfikacji
(np. dokumenty tak, cena/specjalizacje nie).

Publiczność profilu = verificationStatus === approved
published nie istnieje.

⸻

7. Matches — jakość dopasowania
	•	Walidacja zakresu dat:
	•	poprawny format,
	•	from <= to,
	•	maksymalny zakres (np. 30 dni).
	•	Punktowanie terapeutów (scoring), zamiast prostego filtrowania.
	•	Wagi scoringu:
	•	specjalizacja,
	•	dostępność,
	•	rating.

⸻

8. Rating — reguły i moderacja
	•	Reguły:
	•	wymagany komentarz przy ocenie ≤ 3.
	•	Moderacja:
	•	zgłaszanie komentarzy,
	•	ukrywanie bez usuwania.
	•	(Opcjonalnie) odpowiedź terapeuty na komentarz.

Transakcja + update średniej → JUŻ JEST.

⸻

9. Admin / rozliczenia
	•	Panel admina:
	•	akceptacja / odrzucanie terapeutów,
	•	podgląd meetingów i płatności,
	•	statystyki.
	•	Rozliczenia:
	•	saldo terapeuty,
	•	prowizje,
	•	payouty.
	•	Dane formalne:
	•	PESEL / NIP,
	•	adres,
	•	zgody regulaminowe.